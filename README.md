### build in windows:
> cd cmake_test
> mkdir build
> cd build
> cmake -G "MinGW Makefiles" ..
> make

### build in linux:
> cd cmake_test
> mkdir build && cd build
> cmake -G "Unix Makefiles" ..
> make